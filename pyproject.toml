[project]
name = "trendwatcher"
version = "0.1.0"
description = "Real-time cross-platform trend analysis toolkit"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
